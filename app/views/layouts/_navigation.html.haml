= link_to "Geo Tickets", root_path, class: 'brand'

%ul.nav
  - if current_user
    %li
      = link_to(Epic.model_name.human.pluralize, epics_path)
    %li
      = link_to('Tickets', tickets_path)
    %li
      = link_to('Tags', tags_path)
%ul.nav.pull-right
  - if current_user
    %li
      = link_to(user_path(current_user.id), method: 'delete') do
        %i.icon-signout
        Logout
